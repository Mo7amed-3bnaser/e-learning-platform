# 📋 مراجعة شاملة لمنصة مسار التعليمية (E-Learning Platform)

> **ملاحظة:** هذه المنصة تجريبية للتدريب والتعلم وليست منصة إنتاجية أساسية.
>
> **تاريخ المراجعة:** 19 فبراير 2026

---

## 📑 فهرس المحتويات

1. [نظرة عامة على المشروع](#1--نظرة-عامة-على-المشروع)
2. [الميزات الموجودة حالياً](#2--الميزات-الموجودة-حالياً)
3. [الميزات الناقصة](#3--الميزات-الناقصة)
4. [مشاكل الأمان (Security Issues)](#4--مشاكل-الأمان-security-issues)
5. [مشاكل جودة الكود](#5--مشاكل-جودة-الكود)
6. [حالة الاختبارات (Testing)](#6--حالة-الاختبارات-testing)
7. [مشاكل الـ Front-End](#7--مشاكل-الـ-front-end)
8. [توصيات وأولويات الإصلاح](#8--توصيات-وأولويات-الإصلاح)

---

## 1. 🔍 نظرة عامة على المشروع

### التقنيات المستخدمة

| الجزء | التقنية |
|-------|---------|
| **Back-End** | Node.js + Express.js |
| **Front-End** | Next.js 16 + React 19 + TypeScript |
| **قاعدة البيانات** | MongoDB + Mongoose |
| **إدارة الحالة** | Zustand (مع persist) |
| **التنسيق** | Tailwind CSS v4 |
| **الرفع** | Cloudinary |
| **الفيديو** | YouTube + Bunny.net |
| **الاختبارات** | Jest + Testing Library |
| **الأمان** | JWT + Helmet + mongo-sanitize |
| **التسجيل** | Winston + Morgan |

### هيكل المشروع

| الطبقة | عدد الملفات | الوصف |
|--------|-------------|-------|
| Models | 8 | User, Course, Video, Order, Comment, Review, Notification, InstructorApplication |
| Controllers | 12 | كل الـ CRUD + منطق العمل |
| Routes | 14 | ربط الـ endpoints بالـ controllers |
| Middleware | 7 | Auth, Error, Logging, Rate Limiting, Validation |
| Front-End Pages | 33 | صفحات كاملة للطلاب والمدربين والأدمن |
| Components | 35+ | مكونات UI قابلة لإعادة الاستخدام |
| Stores | 4 | Auth, Progress, Theme, Wishlist |

---

## 2. ✅ الميزات الموجودة حالياً

### المصادقة والتفويض (Authentication & Authorization)
- [x] تسجيل حساب جديد مع التحقق من البريد الإلكتروني
- [x] تسجيل الدخول مع قفل الحساب بعد 5 محاولات فاشلة
- [x] نسيت كلمة المرور + إعادة تعيينها
- [x] نظام الأدوار (طالب / مدرب / أدمن)
- [x] JWT Bearer Token Authentication
- [x] تحديث الملف الشخصي ورفع الصورة الشخصية

### الكورسات
- [x] إنشاء / تعديل / حذف الكورسات
- [x] النشر وإلغاء النشر
- [x] البحث والتصفية حسب التصنيف والمستوى
- [x] Pagination في الـ API
- [x] صفحة تفاصيل الكورس

### الفيديوهات
- [x] دعم YouTube و Bunny.net
- [x] معاينة مجانية (Free Preview)
- [x] التحكم في ترتيب الفيديوهات
- [x] مشغل فيديو مدمج

### الطلبات والدفع
- [x] نظام دفع يدوي (رفع إيصال → موافقة الأدمن)
- [x] Sandbox للتسجيل الفوري (بدون دفع)
- [x] تتبع حالة الطلبات

### تتبع التقدم
- [x] تحديد الفيديوهات كمكتملة
- [x] تتبع وقت المشاهدة
- [x] نسبة التقدم الإجمالية
- [x] آخر فيديو تمت مشاهدته

### الشهادات
- [x] إنشاء تلقائي عند إتمام 100%
- [x] توليد PDF
- [x] رفع على Cloudinary
- [x] رابط تحقق عام

### التقييمات والتعليقات
- [x] تقييم الكورسات (1-5 نجوم)
- [x] تعليقات على الفيديوهات
- [x] حساب متوسط التقييم تلقائياً

### الإشعارات
- [x] إشعارات داخلية (أحداث الطلبات والشهادات)
- [x] قراءة / عدم قراءة
- [x] حذف فردي وجماعي

### قائمة الأمنيات (Wishlist)
- [x] إضافة / حذف / فحص الكورسات

### طلبات المدربين
- [x] تقديم طلب عام
- [x] مراجعة الأدمن (قبول / رفض)

### لوحة الأدمن
- [x] إحصائيات عامة
- [x] إدارة الطلاب (حظر / حذف)
- [x] إدارة الطلبات
- [x] إدارة الكورسات
- [x] مراجعة طلبات المدربين

### ميزات إضافية
- [x] الوضع المظلم (Dark Mode)
- [x] أنيميشن وتأثيرات (Framer Motion)
- [x] Error Boundary
- [x] Loading Skeletons
- [x] صفحة 404 مخصصة
- [x] صفحة Privacy و Terms

---

## 3. ❌ الميزات الناقصة

### 🔴 ناقصة - مهمة

| # | الميزة | الوصف |
|---|--------|-------|
| 1 | **Refresh Token** | JWT ينتهي بعد ساعة بدون آلية تجديد — المستخدم لازم يسجل دخول من جديد كل ساعة. الكود فيه تعليق "implement refresh tokens in the future" |
| 2 | **بوابة دفع حقيقية** | فقط دفع يدوي (رفع صورة) و sandbox — لا يوجد Stripe أو PayPal أو فوري |
| 3 | **نظام كوبونات/خصومات** | مش موجود نهائياً |
| 4 | **حذف الحساب (GDPR)** | لا يوجد endpoint لحذف حساب المستخدم |
| 5 | **2FA / MFA** | لا يوجد مصادقة ثنائية |
| 6 | **WebSocket / Real-time** | الإشعارات polling فقط — مفيش WebSocket |
| 7 | **Pagination في الفرونت** | صفحة الكورسات بتجيب كل الكورسات مرة واحدة |
| 8 | **Health Check Endpoint** | مذكور في الـ httpLogger لكن مش مُنفذ |
| 9 | ~~**Graceful Shutdown**~~ ✅ **تم الإصلاح** | ~~لا يوجد signal handlers لـ SIGTERM/SIGINT~~ أضفنا `SIGTERM`/`SIGINT` handlers مع إغلاق الـ HTTP server والـ MongoDB connection |

### 🟡 ناقصة - تحسينية

| # | الميزة | الوصف |
|---|--------|-------|
| 10 | **ردود على التعليقات (Threading)** | التعليقات flat فقط — مفيش ردود متداخلة |
| 11 | **نظام إدارة التصنيفات** | التصنيفات hardcoded في الـ schema enum |
| 12 | **بحث متقدم / Elasticsearch** | بحث بسيط بـ `$regex` فقط |
| 13 | **Autocomplete في البحث** | مش موجود |
| 14 | **تسجيل جماعي (Bulk Enrollment)** | مش موجود |
| 15 | **لوحة تحليلات متقدمة** | فقط إحصائيات أساسية (عدد الطلاب/الكورسات/الإيرادات) |
| 16 | **تتبع إيرادات المدربين / المدفوعات** | مش موجود |
| 17 | **نظام مسودات الكورس** | فقط toggle نشر/إلغاء نشر |
| 18 | **إعادة ترتيب الفيديوهات (Drag & Drop API)** | لا يوجد endpoint لإعادة الترتيب الجماعي |
| 19 | **إبلاغ / تعديل المحتوى (Content Moderation)** | مش موجود |
| 20 | **سجل عمليات الأدمن (Audit Log)** | مش موجود |
| 21 | **تنظيف الإشعارات القديمة** | الـ method `deleteOldNotifications` موجود في الـ model لكن مش بيتنادى (لا يوجد cron job) |
| 22 | **ملف `.env.example`** | لا يوجد ملف نموذجي لمتغيرات البيئة |
| 23 | **Rate Limiting على كل الـ Endpoints** | Rate limiting فقط على login/register/forgot-password/orders |
| 24 | **تصدير البيانات (Export)** | لا يمكن تصدير بيانات الطلاب أو الطلبات |
| 25 | **نظام رسائل بين المستخدمين** | مش موجود |
| 26 | **Quiz / اختبارات** | مش موجود — ميزة مهمة لمنصة تعليمية |
| 27 | **نظام مستويات/إنجازات (Gamification)** | مش موجود |
| 28 | **دعم لغات متعددة (i18n)** | المنصة عربية فقط بدون نظام ترجمة |

---

## 4. 🔒 مشاكل الأمان (Security Issues)

### 🔴 حرجة (Critical)

| # | المشكلة | الملف | التفاصيل |
|---|---------|-------|----------|
| 1 | ~~**JWT يحتوي بيانات شخصية**~~ ✅ **تم الإصلاح** | `Back-End/utils/authHelpers.js` | ~~الـ token payload فيه `name` و `phone` — JWT بيتفك بـ base64 بسهولة.~~ دلوقتي فيه `id` و `role` بس |
| 2 | ~~**كلمة المرور بتتخزن في localStorage كنص عادي**~~ ✅ **تم الإصلاح** | `Front-End/src/app/login/page.tsx` | ~~ميزة "تذكرني" بتحفظ الباسورد الفعلي في cleartext في localStorage.~~ دلوقتي بتحفظ الإيميل بس |
| 3 | ~~**Sandbox Payment بدون حماية بيئة**~~ ✅ **تم الإصلاح** | `Back-End/controllers/sandboxController.js` | ~~`POST /api/orders/sandbox/pay` بيسجل الطالب مجاناً في أي بيئة.~~ دلوقتي محظور في `production` |
| 4 | ~~**أي مستخدم يقدر يحذف أي صورة**~~ ✅ **تم الإصلاح** | `Back-End/routes/uploadRoutes.js` | ~~حذف صور Cloudinary بالـ `publicId` بدون التحقق من الملكية.~~ دلوقتي بيتأكد من الملكية قبل الحذف |
| 5 | ~~**`collection.insertOne` بيتجاوز Mongoose**~~ ✅ **تم الإصلاح** | `Back-End/controllers/instructorApplicationController.js` | ~~إنشاء حساب المدرب الجديد بيتجاوز كل الـ validation والـ middleware.~~ دلوقتي بيستخدم `User.create()` |
| 6 | ~~**لا يوجد حد لحجم الـ request body**~~ ✅ **تم الإصلاح** | `Back-End/server.js` | ~~`express.json()` بدون `limit`.~~ دلوقتي `limit: '5mb'` |
| 7 | ~~**الملفات المرفوعة متاحة للجميع**~~ ✅ **تم الإصلاح** | `Back-End/server.js` | ~~`app.use("/uploads", express.static("uploads"))` — بدون أي تحقق.~~ دلوقتي متاح في development بس |

### 🟠 متوسطة (Moderate)

| # | المشكلة | التفاصيل |
|---|---------|----------|
| 8 | ~~**لا يوجد XSS sanitization**~~ ✅ **تم الإصلاح** | ~~التعليقات والوصف ممكن تحتوي HTML/JS ضار.~~ دلوقتي بيتعمل sanitize بـ `xss` package قبل الحفظ |
| 9 | ~~**لا يوجد Token Blacklisting**~~ ✅ **تم الإصلاح** | ~~لو JWT اتسرق بيفضل شغال.~~ أضفنا `passwordChangedAt` في User model — أي token صدر قبل تغيير الباسورد بيترفض تلقائياً |
| 10 | ~~**تناقض في طول كلمة المرور**~~ ✅ **تم الإصلاح** | ~~Model يقول 6، Validation يقول 10.~~ وحّدنا على **8 حروف** في الاتنين |
| 11 | ~~**`getUserById` بيسرب الإيميل**~~ ✅ **تم الإصلاح** | ~~`GET /api/users/:id` بيرجع الإيميل لأي حد.~~ شيلنا الإيميل من الـ response العام |
| 12 | ~~**Bunny Video IDs بتتكشف**~~ ✅ **تم الإصلاح** | ~~`bunnyVideoId` بيترجع للطلاب.~~ دلوقتي بيترجع فقط للـ admin والـ instructor |
| 13 | ~~**لا يوجد enrollment check على التعليقات**~~ ✅ **تم الإصلاح** | ~~أي مستخدم مسجل يعلق.~~ الطالب لازم يكون مشترك في الكورس عشان يعلق |
| 14 | ~~**3 طرق مختلفة لاسترجاع Auth Token**~~ ✅ **تم الإصلاح** | ~~ملفات API بتقرأ التوكن بطرق مختلفة.~~ وحّدنا القراءة من Zustand persist (`auth-storage`) |
| 15 | ~~**Notification API auth مكسورة**~~ ✅ **تم الإصلاح** | ~~بتقرأ من `localStorage.getItem('token')` مكسور.~~ دلوقتي بتقرأ من `auth-storage` صح |
| 16 | **لا يوجد CSRF protection** | حالياً Bearer token فقط (خطر منخفض)، لكن لو اتضاف cookies هيبقى مشكلة |
| 17 | ~~**Rate limiter مبني على IP فقط**~~ ✅ **تم الإصلاح** | ~~ممكن يتتجاوز بـ VPN.~~ دلوقتي الـ key = IP + email معاً |
| 18 | ~~**`isEmailVerified` مش بيترجع لـ false عند تغيير الإيميل**~~ ✅ **تم الإصلاح** | ~~الإيميل الجديد بيتعتبر متحقق تلقائياً.~~ دلوقتي عند تغيير الإيميل `isEmailVerified` بترجع `false` |

### 🟡 منخفضة (Low)

| # | المشكلة | التفاصيل |
|---|---------|----------|
| 19 | **لا يوجد ملف `.env`** | مفيش `.env.example` للمتغيرات البيئة المطلوبة |
| 20 | **API URL hardcoded** | `src/lib/api.ts` فيه `http://localhost:5000/api` hardcoded — مش بيستخدم env var |
| 21 | **لا يوجد فحص على env vars في Cloudinary config** | لو المتغيرات مش موجودة، بيفشل بصمت |
| 22 | ~~**Dashboard بيحمل كل الـ orders في الذاكرة**~~ ✅ **تم الإصلاح** | ~~`adminController.js` بيعمل `reduce` على كل الأوردرات بدل MongoDB aggregation~~ دلوقتي بيستخدم `Order.aggregate()` |

---

## 5. 🔧 مشاكل جودة الكود

### 5.1 ~~Error Handling غير موحد~~ ✅ تم الإصلاح

| المشكلة | التفاصيل |
|---------|----------|
| ~~**نمطين مختلفين**~~ ✅ | ~~بعض الـ controllers بتستخدم `asyncHandler` والبعض الآخر `try/catch` يدوي~~ كل الـ controllers دلوقتي بتستخدم `asyncHandler` |
| ~~**تسريب رسائل الخطأ**~~ ✅ | ~~الـ controllers اللي بتستخدم `try/catch` يدوي بتسرب `error.message` للمستخدم~~ دلوقتي كل الأخطاء بتتمرر للـ `errorMiddleware` |
| ~~**`console.error` بدل logger**~~ ✅ | ~~بعض الـ controllers بتستخدم `console.error` بدل Winston logger المعد~~ كل الـ `console.error` اتبدلت بـ `logger.error` |

### 5.2 ~~تكرار الكود (Code Duplication)~~ ✅ تم الإصلاح (جزئياً)

| النمط المتكرر | عدد المواقع |
|---------------|-------------|
| ~~فحص التسجيل في الكورس (enrollment check)~~ ✅ | ~~8+ أماكن مكررة عبر 6 controllers~~ تم استخراجه لـ `utils/enrollmentHelper.js` |
| التحقق من ملكية الكورس | مكرر في `videoController.js` رغم وجود middleware |
| ~~فحص `isEnrolled` بـ `find`/`some`~~ ✅ | ~~6+ controllers~~ دلوقتي بيستخدموا `enrollmentHelper` |

### 5.3 مشاكل النماذج (Data Model Issues) — ✅ جزئياً

| المشكلة | الملف |
|---------|-------|
| `userName` و `userAvatar` متكررة في Comment و Review | لو المستخدم غير اسمه، التعليقات القديمة تبقى باسم قديم |
| ~~`enrolledCourses` بتستخدم `Schema.Types.Mixed`~~ ✅ | ~~فقدان type safety — المفروض sub-schema~~ تم إنشاء `videoProgressSchema` + `enrolledCourseSchema` |
| `enrolledStudents` بيتحدث يدوياً | خطر عدم التزامن مع الأوردرات الفعلية |

### 5.4 مشاكل أخرى

| المشكلة | التفاصيل |
|---------|----------|
| **مفيش TypeScript في الـ Back-End** | كل الكود JavaScript بدون type checking |
| ~~**Magic strings في كل مكان**~~ ✅ | ~~أسماء الأدوار، الحالات — المفروض constants~~ تم إنشاء `utils/constants.js` |
| ~~**ترتيب الـ routes غلط**~~ ✅ | ~~`/:id` قبل `/instructor/my-courses`~~ Static routes دلوقتي قبل Dynamic routes |
| ~~**لا يوجد Graceful Shutdown**~~ ✅ | ~~مفيش signal handlers~~ أضفنا `SIGTERM`/`SIGINT` handlers |
| ~~**`connectDB()` مش awaited**~~ ✅ | ~~لو الاتصال فشل، الـ app بيبدأ~~ دلوقتي `await connectDB()` قبل `app.listen()` |
| ~~**`sendEmail` بينشئ transporter جديد كل مرة**~~ ✅ | ~~المفروض يعيد استخدام الاتصال~~ دلوقتي singleton transporter مع `pool: true` |
| **Race Condition في الأوردرات** | لو أدمنين وافقوا على نفس الأوردر في نفس الوقت، الطالب بيتسجل مرتين |

---

## 6. 🧪 حالة الاختبارات (Testing)

### Back-End

#### ✅ اختبارات موجودة (Unit Tests)

| الملف | التغطية |
|-------|---------|
| `authController.test.js` | Register, Login, GetMe, UpdateProfile, ForgotPassword, ResetPassword |
| `adminController.test.js` | Dashboard stats |
| `courseController.test.js` | Course CRUD |
| `orderController.test.js` | Order flow |
| `videoController.test.js` | Video management |
| `authMiddleware.test.js` | Auth middleware |
| `validation.test.js` | Validation rules |
| `authHelpers.test.js` | JWT helpers |
| `pagination.test.js` | Pagination utility |

#### ✅ اختبارات موجودة (Integration Tests)

| الملف | التغطية |
|-------|---------|
| `auth.test.js` | Registration, Login, Profile |
| `courses.test.js` | Course CRUD |
| `orders.test.js` | Order flow |
| `admin.test.js` | Admin dashboard |

#### ❌ اختبارات ناقصة (Back-End)

| الناقص | النوع |
|--------|-------|
| `commentController` | Unit |
| `progressController` | Unit |
| `certificateController` | Unit |
| `reviewController` | Unit |
| `notificationController` | Unit |
| `wishlistController` | Unit |
| `sandboxController` | Unit |
| `instructorApplicationController` | Unit |
| `instructorAuth` middleware | Unit |
| `optionalAuth` middleware | Unit |
| `rateLimiter` | Unit |
| `certificateGenerator` utility | Unit |
| `sendEmail` utility | Unit |
| **كل الـ Models** (validation, hooks, methods) | Unit |
| Flow كامل: طلب → موافقة → مشاهدة → تقدم → شهادة | Integration |
| طلب مدرب → موافقة → إنشاء كورس | Integration |
| التقييم: إكمال الكورس → تقييم → تحديث المتوسط | Integration |
| Rate limiter behavior | Integration |
| رفع/حذف الملفات | Integration |
| Wishlist operations | Integration |
| Certificate generation + verification | Integration |
| التعليقات CRUD | Integration |
| Edge cases: حسابات محظورة، tokens منتهية، طلبات متزامنة | Integration |

### Front-End

#### ✅ اختبارات موجودة

| الملف | التغطية |
|-------|---------|
| `CourseProgressBar.test.tsx` | Component test |
| `EmptyState.test.tsx` | Component test |
| `Loading.test.tsx` | Component test |
| `LoadingButton.test.tsx` | Component test |
| `StarRating.test.tsx` | Component test |
| `authStore.test.ts` | Store test |

#### ❌ اختبارات ناقصة (Front-End)

| الناقص |
|--------|
| 29 component بدون اختبارات |
| 3 stores ناقصة (progressStore, themeStore, wishlistStore) |
| 2 hooks بدون اختبارات (useRecentlyViewed, useVideoBookmark) |
| 3 API service files بدون اختبارات |
| كل الصفحات (33 صفحة) بدون اختبارات |
| لا يوجد E2E tests (Cypress/Playwright) |

### 📊 تقدير تغطية الاختبارات

| الجزء | التغطية التقريبية |
|-------|-------------------|
| Back-End Unit | ~35% |
| Back-End Integration | ~20% |
| Front-End Components | ~15% |
| Front-End Pages | 0% |
| E2E | 0% |
| **الإجمالي التقريبي** | **~15-20%** |

---

## 7. 💻 مشاكل الـ Front-End

### 7.1 مشاكل الأداء (Performance)

| المشكلة | التفاصيل |
|---------|----------|
| **لا يوجد Pagination في صفحة الكورسات** | بتجيب كل الكورسات مرة واحدة من الـ API |
| **لا يوجد Image Optimization** | مفيش lazy loading أو تحسين للصور |
| **لا يوجد Code Splitting يدوي** | الاعتماد فقط على Next.js التلقائي |
| **لا يوجد Caching Strategy** | مفيش SWR أو React Query للـ data caching |
| **Re-renders زايدة** | بعض الـ components مش بتستخدم `memo` أو `useCallback` |

### 7.2 مشاكل الوصولية (Accessibility)

| المشكلة | التفاصيل |
|---------|----------|
| **بعض الـ buttons بدون aria labels** | خصوصاً أزرار الأيقونات |
| **لا يوجد skip navigation** | مفيش رابط تخطي للمحتوى الرئيسي |
| **نسب تباين الألوان** | بعض الألوان ممكن تكون ضعيفة التباين |
| **لا يوجد focus management** | بعد الـ navigation مفيش إدارة للـ focus |

### 7.3 مشاكل أخرى

| المشكلة | التفاصيل |
|---------|----------|
| **API URL hardcoded** | `http://localhost:5000/api` في `api.ts` بدل env variable |
| **3 أنماط مختلفة لاسترجاع التوكن** | كل ملف API بيقرأ التوكن بطريقة مختلفة |
| **مفيش SEO optimization** | لا يوجد metadata ديناميكي أو structured data |
| **مفيش PWA support** | لا يوجد service worker أو manifest |
| **مفيش Error Recovery** | ErrorBoundary موجود بس مفيش retry mechanisms |

---

## 8. 📌 خطة العمل — Phases & Tasks

> كل Phase مقسمة لتاسكات واضحة. ابدأ بالترتيب — كل phase لازم تخلص قبل اللي بعدها.
> علّم على كل تاسك ✅ لما تخلصه.

---

### 🔴 Phase 1 — إصلاحات أمنية حرجة (Critical Security Fixes)
> **الأولوية:** عالية جداً | **الوقت المقدر:** 1-2 يوم
> **الهدف:** سد كل الثغرات الأمنية الخطيرة حتى لو المنصة تجريبية

| # | Task | الملف المطلوب تعديله | الحالة |
|---|------|----------------------|--------|
| 1.1 | إزالة حفظ الباسورد في localStorage (ميزة "تذكرني") — الاكتفاء بحفظ الإيميل فقط أو استخدام Remember Me Token | `Front-End/src/app/login/page.tsx` | ✅ |
| 1.2 | إزالة `name` و `phone` من JWT payload — الإبقاء على `id` و `role` فقط | `Back-End/utils/authHelpers.js` | ✅ |
| 1.3 | إضافة environment guard للـ sandbox — يشتغل في `development` بس | `Back-End/controllers/sandboxController.js` | ✅ |
| 1.4 | إضافة ownership check لحذف صور Cloudinary — التحقق إن المستخدم مالك الصورة | `Back-End/routes/uploadRoutes.js` | ✅ |
| 1.5 | إصلاح `collection.insertOne` واستبداله بـ `User.create()` في الموافقة على طلب المدرب | `Back-End/controllers/instructorApplicationController.js` | ✅ |
| 1.6 | إضافة `{ limit: '1mb' }` لـ `express.json()` | `Back-End/server.js` | ✅ |
| 1.7 | إضافة auth middleware على `/uploads` static files أو نقلها لـ Cloudinary فقط | `Back-End/server.js` | ✅ |

---

### 🟠 Phase 2 — إصلاحات أمنية متوسطة + Bugs
> **الأولوية:** عالية | **الوقت المقدر:** 2-3 أيام
> **الهدف:** إصلاح الثغرات المتوسطة والأخطاء المنطقية

| # | Task | الملف المطلوب تعديله | الحالة |
|---|------|----------------------|--------|
| 2.1 | إصلاح `isEmailVerified` — يرجع `false` لما المستخدم يغير إيميله | `Back-End/controllers/authController.js` | ✅ |
| 2.2 | إصلاح `addVideoValidation` — `bunnyVideoId` يبقى optional لو `videoProvider === 'youtube'` | `Back-End/middleware/validation.js` | ✅ |
| 2.3 | إخفاء الإيميل في `getUserById` — عدم إرجاع `email` في الـ public endpoint | `Back-End/controllers/authController.js` | ✅ |
| 2.4 | إضافة enrollment check على التعليقات — التحقق إن المستخدم مشترك قبل التعليق | `Back-End/controllers/commentController.js` | ✅ |
| 2.5 | إصلاح Race Condition في الأوردرات — استخدام `findOneAndUpdate` مع `{ status: 'pending' }` | `Back-End/controllers/orderController.js` | ✅ |
| 2.6 | إضافة XSS sanitization — تنصيب واستخدام `xss-clean` أو `sanitize-html` | `Back-End/server.js` + `package.json` | ✅ |
| 2.7 | توحيد طول كلمة المرور — Model و Validation يكونوا نفس القيمة (10 حروف) | `Back-End/models/User.js` + `Back-End/middleware/validation.js` | ✅ |
| 2.8 | إخفاء `bunnyVideoId` من الـ response — عمل proxy أو عدم إرجاعه | `Back-End/controllers/videoController.js` | ✅ |
| 2.9 | منع الاشتراك في كورسات غير منشورة — إضافة `isPublished` check في الطلبات | `Back-End/controllers/orderController.js` | ✅ |

---

### 🟡 Phase 3 — إصلاحات الفرونت إند
> **الأولوية:** متوسطة-عالية | **الوقت المقدر:** 2-3 أيام
> **الهدف:** إصلاح الأخطاء والمشاكل في الواجهة الأمامية

| # | Task | الملف المطلوب تعديله | الحالة |
|---|------|----------------------|--------|
| 3.1 | توحيد استرجاع Auth Token — الـ 3 ملفات API تستخدم نفس الطريقة من Zustand store | `Front-End/src/lib/api.ts` + `instructorApi.ts` + `notificationsApi.ts` | ✅ |
| 3.2 | إصلاح API URL hardcoded — استخدام `NEXT_PUBLIC_API_URL` env variable | `Front-End/src/lib/api.ts` | ✅ |
| 3.3 | إصلاح Notification API auth — قراءة التوكن من `auth-storage` بشكل صحيح | `Front-End/src/lib/notificationsApi.ts` | ✅ |
| 3.4 | إضافة Pagination في صفحة الكورسات — تحميل الكورسات على دفعات | `Front-End/src/app/courses/page.tsx` | ✅ |
| 3.5 | إنشاء ملف `.env.example` للفرونت والباكند — توثيق كل المتغيرات المطلوبة | `Front-End/.env.example` + `Back-End/.env.example` | ✅ |
| 3.6 | إضافة Error Recovery — retry mechanisms في API calls الفاشلة | `Front-End/src/lib/api.ts` | ✅ |

---

### 🔵 Phase 4 — توحيد وتنظيف الكود (Code Quality)
> **الأولوية:** متوسطة | **الوقت المقدر:** 3-4 أيام
> **الهدف:** كود أنظف وأسهل في الصيانة

| # | Task | التفاصيل | الحالة |
|---|------|----------|--------|
| 4.1 | ~~توحيد Error Handling — كل الـ controllers تستخدم `asyncHandler`~~ | ~~تحويل `commentController`, `progressController`, `notificationController`~~ | ✅ |
| 4.2 | ~~استبدال `console.error` بـ `logger.error`~~ | ~~كل الـ controllers اللي بتستخدم `console.error`~~ | ✅ |
| 4.3 | ~~استخراج enrollment check كـ utility function مشتركة~~ | ~~إنشاء `Back-End/utils/enrollmentHelper.js` واستخدامه في 6+ controllers~~ | ✅ |
| 4.4 | ~~استبدال Magic Strings بـ Constants~~ | ~~إنشاء `Back-End/utils/constants.js` — أدوار، حالات، رسائل خطأ~~ | ✅ |
| 4.5 | ~~إصلاح ترتيب الـ Routes — Static routes قبل Dynamic routes~~ | ~~`Back-End/routes/courseRoutes.js` — `/instructor/my-courses` قبل `/:id`~~ | ✅ |
| 4.6 | ~~إصلاح `enrolledCourses` — استبدال `Schema.Types.Mixed` بـ proper sub-schema~~ | ~~`Back-End/models/User.js`~~ | ✅ |
| 4.7 | ~~إضافة `isMongoId()` validation — لكل الـ `:id` parameters في كل الـ routes~~ | ~~كل ملفات الـ routes~~ | ✅ |
| 4.8 | ~~إصلاح Revenue Calculation — استخدام MongoDB `$group` + `$sum` aggregation~~ | ~~`Back-End/controllers/adminController.js`~~ | ✅ |
| 4.9 | ~~إعادة استخدام Email Transporter — connection pooling بدل إنشاء transporter كل مرة~~ | ~~`Back-End/utils/sendEmail.js`~~ | ✅ |
| 4.10 | ~~Await `connectDB()` — انتظار اتصال قاعدة البيانات قبل بدء الـ server~~ | ~~`Back-End/server.js`~~ | ✅ |

---

### 🟣 Phase 5 — تحسينات البنية التحتية (Infrastructure)
> **الأولوية:** متوسطة | **الوقت المقدر:** 2-3 أيام
> **الهدف:** استقرار أفضل وأدوات مراقبة

| # | Task | التفاصيل | الحالة |
|---|------|----------|--------|
| 5.1 | إضافة Refresh Token mechanism | JWT access token (15 دقيقة) + refresh token (7 أيام) مع endpoint `/auth/refresh` | ⬜ |
| 5.2 | إضافة Token Blacklisting | تخزين التوكنات الملغية في Redis أو MongoDB — تبطل عند تغيير الباسورد أو logout | ✅ |
| 5.3 | إضافة Health Check endpoint | `GET /api/health` — يرجع حالة الـ DB والـ app | ⬜ |
| 5.4 | ~~إضافة Graceful Shutdown~~ | ~~Signal handlers لـ `SIGTERM` و `SIGINT` — إغلاق الـ DB connection والـ server بأمان~~ | ✅ |
| 5.5 | إضافة Rate Limiting شامل | Rate limiting على كل الـ API endpoints مش login بس | ⬜ |
| 5.6 | جدولة تنظيف الإشعارات القديمة | Cron job يستدعي `Notification.deleteOldNotifications()` كل يوم | ⬜ |
| 5.7 | إضافة Environment Variables Validation | فحص كل الـ env vars المطلوبة عند بدء التشغيل — يرمي error لو ناقص حاجة | ⬜ |
| 5.8 | إضافة CORS configuration أفضل | تحديد الـ allowed origins بدقة بدل wildcard | ⬜ |

---

### ⚪ Phase 6 — الاختبارات (Testing)
> **الأولوية:** متوسطة-منخفضة | **الوقت المقدر:** 5-7 أيام
> **الهدف:** وصول تغطية الاختبارات لـ 60%+

#### 6A — Unit Tests (Back-End)

| # | Task | الملف المطلوب إنشاؤه | الحالة |
|---|------|----------------------|--------|
| 6.1 | اختبارات `commentController` | `Back-End/tests/unit/commentController.test.js` | ⬜ |
| 6.2 | اختبارات `progressController` | `Back-End/tests/unit/progressController.test.js` | ⬜ |
| 6.3 | اختبارات `certificateController` | `Back-End/tests/unit/certificateController.test.js` | ⬜ |
| 6.4 | اختبارات `reviewController` | `Back-End/tests/unit/reviewController.test.js` | ⬜ |
| 6.5 | اختبارات `notificationController` | `Back-End/tests/unit/notificationController.test.js` | ⬜ |
| 6.6 | اختبارات `wishlistController` | `Back-End/tests/unit/wishlistController.test.js` | ⬜ |
| 6.7 | اختبارات `sandboxController` | `Back-End/tests/unit/sandboxController.test.js` | ⬜ |
| 6.8 | اختبارات `instructorApplicationController` | `Back-End/tests/unit/instructorApplicationController.test.js` | ⬜ |
| 6.9 | اختبارات `instructorAuth` middleware | `Back-End/tests/unit/instructorAuth.test.js` | ⬜ |
| 6.10 | اختبارات `rateLimiter` middleware | `Back-End/tests/unit/rateLimiter.test.js` | ⬜ |
| 6.11 | اختبارات `certificateGenerator` utility | `Back-End/tests/unit/certificateGenerator.test.js` | ⬜ |
| 6.12 | اختبارات `sendEmail` utility | `Back-End/tests/unit/sendEmail.test.js` | ⬜ |
| 6.13 | اختبارات Models (validation, hooks) | `Back-End/tests/unit/models/*.test.js` | ⬜ |

#### 6B — Integration Tests (Back-End)

| # | Task | التفاصيل | الحالة |
|---|------|----------|--------|
| 6.14 | Full Enrollment Flow | طلب → موافقة → مشاهدة → تقدم → شهادة | ⬜ |
| 6.15 | Instructor Application Flow | طلب مدرب → موافقة → إنشاء كورس | ⬜ |
| 6.16 | Review Flow | إكمال كورس → تقييم → تحديث المتوسط | ⬜ |
| 6.17 | Comment CRUD | إنشاء / تعديل / حذف تعليقات | ⬜ |
| 6.18 | Wishlist Operations | إضافة / حذف / فحص | ⬜ |
| 6.19 | Certificate Generation + Verification | توليد PDF + التحقق العام | ⬜ |

#### 6C — Front-End Tests

| # | Task | التفاصيل | الحالة |
|---|------|----------|--------|
| 6.20 | اختبارات باقي الـ Components (29 component) | `Front-End/tests/components/*.test.tsx` | ⬜ |
| 6.21 | اختبارات الـ Stores الناقصة | `progressStore`, `themeStore`, `wishlistStore` | ⬜ |
| 6.22 | اختبارات الـ Hooks | `useRecentlyViewed`, `useVideoBookmark` | ⬜ |
| 6.23 | اختبارات الـ API Services | `api.ts`, `instructorApi.ts`, `notificationsApi.ts` | ⬜ |

---

### 🌟 Phase 7 — تحسينات الأداء والـ UX
> **الأولوية:** منخفضة | **الوقت المقدر:** 3-4 أيام
> **الهدف:** تجربة مستخدم أسرع وأفضل

| # | Task | التفاصيل | الحالة |
|---|------|----------|--------|
| 7.1 | إضافة React Query أو SWR | Data caching + automatic revalidation | ⬜ |
| 7.2 | إضافة Image Optimization | Next.js `<Image>` component + lazy loading | ⬜ |
| 7.3 | إضافة SEO Metadata ديناميكي | `generateMetadata()` لكل صفحة + structured data | ⬜ |
| 7.4 | تحسين Accessibility | Aria labels + skip navigation + focus management + color contrast | ⬜ |
| 7.5 | إضافة `memo` و `useCallback` للـ Components الكبيرة | تقليل الـ re-renders الزايدة | ⬜ |
| 7.6 | إضافة Loading States أفضل | Skeleton screens لكل الصفحات مش بعضها | ⬜ |

---

### 🚀 Phase 8 — ميزات جديدة (Future Features)
> **الأولوية:** منخفضة | **الوقت المقدر:** حسب الميزة
> **الهدف:** توسيع المنصة بميزات جديدة (اختياري)

| # | Task | الوقت المقدر | الحالة |
|---|------|-------------|--------|
| 8.1 | نظام Quiz / اختبارات | 5-7 أيام | ⬜ |
| 8.2 | بوابة دفع حقيقية (Stripe / Paymob) | 3-5 أيام | ⬜ |
| 8.3 | WebSocket للإشعارات الفورية (Socket.io) | 2-3 أيام | ⬜ |
| 8.4 | ردود متداخلة على التعليقات (Comment Threading) | 2-3 أيام | ⬜ |
| 8.5 | نظام كوبونات وخصومات | 2-3 أيام | ⬜ |
| 8.6 | 2FA / MFA (Google Authenticator أو SMS) | 2-3 أيام | ⬜ |
| 8.7 | بحث متقدم (Elasticsearch / MongoDB Atlas Search) | 3-4 أيام | ⬜ |
| 8.8 | نظام رسائل بين المستخدمين | 4-5 أيام | ⬜ |
| 8.9 | Gamification — مستويات وإنجازات وشارات | 3-5 أيام | ⬜ |
| 8.10 | دعم لغات متعددة (i18n — next-intl) | 3-4 أيام | ⬜ |
| 8.11 | PWA Support (Service Worker + Manifest) | 1-2 يوم | ⬜ |
| 8.12 | Admin Audit Log — سجل كل عمليات الأدمن | 2-3 أيام | ⬜ |
| 8.13 | تصدير البيانات (Export to CSV/Excel) | 1-2 يوم | ⬜ |
| 8.14 | حذف الحساب (GDPR Compliance) | 1-2 يوم | ⬜ |
| 8.15 | لوحة تحليلات متقدمة (Charts + Graphs) | 3-5 أيام | ⬜ |
| 8.16 | إضافة E2E Tests (Cypress أو Playwright) | 5-7 أيام | ⬜ |
| 8.17 | تحويل الـ Back-End لـ TypeScript | 7-10 أيام | ⬜ |

---

## 📊 ملخص الخطة

| Phase | عدد التاسكات | المنجز | الوقت المقدر | الأولوية |
|-------|-------------|--------|-------------|----------|
| **Phase 1** — إصلاحات أمنية حرجة | 7 | ✅ 7/7 **مكتملة** | 1-2 يوم | 🔴 حرجة |
| **Phase 2** — إصلاحات أمنية + Bugs | 9 | ✅ 9/9 **مكتملة** | 2-3 أيام | 🟠 عالية |
| **Phase 3** — إصلاحات الفرونت | 6 | ✅ 6/6 **مكتملة** | 2-3 أيام | 🟡 متوسطة-عالية |
| **Phase 4** — تنظيف الكود | 10 | ✅ 10/10 **مكتملة** | 3-4 أيام | 🔵 متوسطة |
| **Phase 5** — البنية التحتية | 8 | 🔄 2/8 (5.2, 5.4 مكتملين) | 2-3 أيام | 🟣 متوسطة |
| **Phase 6** — الاختبارات | 23 | ⬜ 0/23 | 5-7 أيام | ⚪ متوسطة-منخفضة |
| **Phase 7** — الأداء والـ UX | 6 | ⬜ 0/6 | 3-4 أيام | 🌟 منخفضة |
| **Phase 8** — ميزات جديدة | 17 | ⬜ 0/17 | حسب الميزة | 🚀 اختياري |
| **الإجمالي** | **86 تاسك** | **34/86 منجزة (40%)** | **~20-30 يوم** | — |

---

## 📈 مؤشرات التقدم

| المؤشر | القيمة الحالية | الهدف بعد Phase 4 | الهدف النهائي |
|--------|---------------|-------------------|--------------|
| الميزات المكتملة | ~80% | ✅ ~80% **تم** | ~95% |
| الأمان | ⚠️ 22 مشكلة | ✅ 0 حرجة، 0 متوسطة | ✅ 0 مشاكل |
| تغطية الاختبارات | ~15-20% | ~25% | ~60%+ |
| جودة الكود | ~~متوسطة~~ **جيدة** ✅ | ✅ جيدة **تم** | ممتازة |
| الأداء | يحتاج تحسين | متوسط | جيد |

> **ابدأ بـ Phase 1 — الأمان أولاً، حتى لو المنصة تجريبية. عادات الأمان الجيدة بتتبني من أول مشروع.**

